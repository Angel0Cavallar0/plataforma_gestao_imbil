import { LoadingScreen } from "@/components/shared/loading-screen";

export default function ConfiguracoesLoading() {
  return <LoadingScreen message="Carregando configurações..." showLogo={false} />;
}
